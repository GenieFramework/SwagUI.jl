{
    "swagger": "2.0",
    "paths": {
        "/doge": {
            "get": {
                "responses": { "200": { "description": "Returns a doge." } },
                "description": "doge"
            }
        },
        "/to": {
            "put": {
                "responses": { "200": { "description": "Returns a doge." } },
                "description": "to"
            }
        },
        "/the": {
            "post": {
                "responses": { "200": { "description": "Returns a doge." } },
                "description": "the"
            }
        },
        "/moon": {
            "get": {
                "responses": { "200": { "description": "Returns a moon." } },
                "description": "moon"
            }
        }
    },
    "info": { "title": "Doge to the moon", "version": "1.0.5" }
}
